import React from 'react';
import ReactDOM from 'react-dom/client';

const firstElement= <h1>Hello desde React</h1>
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  firstElement
);

